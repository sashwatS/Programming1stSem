/*
CH-230-A
a1_p4.[c]
Sashwat Sharma
sassharma@jacobs-university.de
*/
/*
#include <stdio.h>
int main() {
    int x = 17;
    int y = 4;
    //declares the x and y variables
    printf("x=%d\n", x);
    printf("y=%d\n", y);
    printf("sum=%d\n", x + y);
    printf("product=%d\n", x * y);
    printf("difference=%d\n", x - y);
    //prints the variables and their sum, product, and difference
    float div = x*1.0/y*1.0;
    //multiplying with 1.0 converts each variable to float so that they can be accurately divided
    printf("division=%f\n", div);
    printf("remainder of division=%d\n", x%y);
    //prints the division and the remainder of division for the variables
    return 0;
 } */