/*
CH-230-A
a6_p2.[c]
Sashwat Sharma
sassharma@jacobs-university.de
*/
/*
#include <stdio.h>
#define LEAST_SIGNIFICANT(n) ((n) & 1)
//compares the argument with 1, returns 0 is false
int main() {
    unsigned char n;
    scanf("%hhu", &n);
    unsigned char bit =LEAST_SIGNIFICANT(n);
    //calls macro
    printf("%u\n", (unsigned int)n);
    printf("%u\n", (unsigned int)bit);
    //prints the decimal value and the least significant bit
    return 0;
}*/
