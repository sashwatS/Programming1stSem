/*
CH-230-A
a6_p1.[c]
Sashwat Sharma
sassharma@jacobs-university.de
*/
/*
#include <stdio.h>
#define SWAP(x, y, T) do { T SWAP = x; x = y; y = SWAP; } while (0)
//T becomes a placeholder for the data type
int main() {
    int a, b;
    double c, d;
    scanf("%d%d", &a, &b);
    scanf("%lf%lf", &c, &d);
    //takes inputs
    SWAP(a, b, int);
    SWAP(c, d, double);
    printf("After swapping:\n");
    printf("%d\n%d\n", a, b);
    printf("%.6lf\n%.6lf\n", c, d);
    //performs macro execution and prints
    return 0;
}
*/
