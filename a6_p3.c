/*
CH-230-A
a6_p3.[c]
Sashwat Sharma
sassharma@jacobs-university.de
*/
/*
#include <stdio.h>
#define MAX(A, B, C) ((A) > (B) ? ((A) > (C) ? (A) : (C)) : ((B) > (C) ? (B) : (C)))
#define MIN(A, B, C) ((A) < (B) ? ((A) < (C) ? (A) : (C)) : ((B) < (C) ? (B) : (C)))
//runs conditions for comparing a, b and c and a nested condition for b and c
#define MIDRANGE(X, Y) ((X + Y) /2.0)
//finds the midrange
int main() {
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    int max = MAX(a, b, c);
    int min = MIN(a, b, c);
    printf("The mid-range is: %.6f\n", MIDRANGE(min, max));
    //the macros are called
}*/
