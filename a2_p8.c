/*
CH-230-A
a2_p8.[c]
Sashwat Sharma
sassharma@jacobs-university.de
*/
/*
#include <stdio.h>
int main() {
    int number;
    char l[100];
    //declares the variable number
    fgets(l, sizeof (l), stdin);
    sscanf(l, "%d", &number);
    //asks user for input
    if ((number % 2 == 0) && (number % 7 == 0)) {
        printf("The number is divisible by 2 and 7\n");
    }
    else {
        printf("The number is NOT divisible by 2 and 7\n");
    }
    //runs conditional statements and prints the appropriate outputs
    return 0;
} */