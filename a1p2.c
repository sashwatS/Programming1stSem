/*
CH-230-A
a1_p2.[c]
Sashwat Sharma
sassharma@jacobs-university.de
*/

#include <stdio.h>
int main() {
    int result; //The result of our calculation
    result = (2 + 7) * 9 / 3;
    printf("The result is %d\n", result);
    //the "results" variable was not added in the print line, causing randoms integers to be printed instead
    return 0;
}
