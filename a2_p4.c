/*
CH-230-A
a2_p4.[c]
Sashwat Sharma
sassharma@jacobs-university.de
*/
/*
#include <stdio.h>
int main() {
    float a, b, h;
    //declares the variables
    scanf("%f", &a);
    getchar();
    scanf("%f", &b);
    getchar();
    scanf("%f", &h);
    //gets the inputs for a, b and h
    printf("square area=%f\n", a*a);
    printf("rectangle area=%f\n", a*b);
    printf("triangle area=%f\n", (a/2)*h);
    printf("trapezoid area=%f\n", ((a+b)/2)*h);
    //calculates the outputs and prints them
    return 0;
} */